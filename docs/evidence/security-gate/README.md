# Evidencia de gate de seguridad bloqueado

Pendiente de la Fase 3: capturar aquí un PR real bloqueado por `reusable-security.yml` (quality gate de SonarQube fallido, hallazgo de Gitleaks, o `release-deploy.yml` detenido por Trivy en CRÍTICO/ALTO). Incluir captura del check en rojo en GitHub y el reporte de la herramienta correspondiente.
